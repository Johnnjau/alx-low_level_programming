#include "main.h"

/**
 * print_number - prints and integer.
 * @n input integer
 * Return: no return.
 */

void print_number(int n);
{
	unsigned int m, d, count;
	if (n < 0
			)
	{
		_putchar(45);
		m = n * -1;
	}
	else
	{
		m = n;
	}
	d = m;
	count = 1;
}
